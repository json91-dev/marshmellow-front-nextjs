import ConfirmButton from '@/app/(beforeLogin)/signup/_components/ConfirmButton';
import React from 'react';

export default function Confirm() {
  return <ConfirmButton text={'채용 결과 보기'} customStyle={{ width: '100%' }} />;
}
